- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.nanorc: nanorc

- create: []

- shell:
    - [git submodule update --init --recursive, Installing submodules]
